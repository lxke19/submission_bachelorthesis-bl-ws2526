# ============================================================
# --- App Secrets ---
# ============================================================
AUTH_SECRET="random-string-64bit"

# ============================================================
# --- Optional 3rd party keys ---
# ============================================================
TAVILY_API_KEY="key"
OPENAI_API_KEY="key"
# LANGSMITH_API_KEY=
# LANGSMITH_TRACING_V2=true
# LANGSMITH_PROJECT=default
# ANTHROPIC_API_KEY=

# ============================================================
# --- LangGraph CHECKPOINTER DB (ONLY)
# One Postgres database, shared by ALL agents.
# Separation is done via SCHEMAS (Option A).
# ============================================================
LANGGRAPH_POSTGRES_URL="postgresql://langgraph:langgraph_pw@localhost:55432/langgraph_db"

# ============================================================
# --- LangGraph AGENT SCHEMAS (Option A)
# Each agent MUST use its own schema to avoid collisions.
# These vars are read by each agent module independently.
# ============================================================

# tavilyAgent
LANGGRAPH_POSTGRES_SCHEMA_TAVILY="lg_tavily_agent"

# dataAwareLLMSystem
LANGGRAPH_POSTGRES_SCHEMA_DATA_AWARE="lg_data_aware_llm_system"

# ============================================================
# --- Default model configuration (shared convention)
# All agents use GPT-5.1
# You can still override per run via RunnableConfig.
# ============================================================
LANGGRAPH_DEFAULT_MODEL="openai/gpt-5.1"

# ============================================================
# --- App DB (ONLY) - Prisma / Migrate uses this via prisma.config.ts
# ============================================================
DATABASE_URL="postgresql://app:app_pw@localhost:55433/app_db"

# ============================================================
# --- Dataset DB (SQL Agent) ---
# Separate Postgres DB containing the ESG dataset (read-only for the agent)
# ============================================================
DATASET_POSTGRES_URL="postgresql://esg:esg_pw@localhost:55434/esg_db"

# SQL Tool safety/perf
DATASET_SQL_MAX_ROWS="500"
DATASET_SQL_STATEMENT_TIMEOUT_MS="8000"

# ============================================================
# --- Mail (optional; if missing, we just log mails)
# ============================================================
SMTP_HOST="smtp.example.com"
SMTP_PORT="port"
SMTP_SECURE="true/false"
SMTP_USER="example.user@test.com"
SMTP_PASS="ttttttttttttttt"

SMTP_FROM_NAME="Example User"
SMTP_FROM_ADDRESS="example.user@test.com"

# ============================================================
# --- Extras for mail content and other texts ---
# ============================================================
APP_BASE_URL="http://localhost:3000"
TOTP_ISSUER="Bachelorthesis Lukas Bieringer"

# ============================================================
# --- Agent Chat UI (LangChain / LangGraph UI)
# ============================================================

# API route exposed by your Next.js app
NEXT_PUBLIC_API_URL="/api/langgraph"

# Default assistant shown in UI (can be overridden via URL param)
# You will likely switch this between:
# - tavilyAgent
# - dataAwareLLMSystem
NEXT_PUBLIC_ASSISTANT_ID="dataAwareLLMSystem"

# Internal LangGraph server URL (healthcheck + streaming)
LANGGRAPH_API_URL="http://localhost:2024"

# CORS
CORS_ALLOWED_ORIGINS="http://localhost:3000,http://localhost:3001"
